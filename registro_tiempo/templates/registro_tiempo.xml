<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="home_parte_horas">
        <t t-call="portal.frontend_layout">
            <div id="o_page_registro_horas" class="container  mb-5">
                <div class="row pt-2 pb-2">
                    <div class="col-md-12 text-center mt-5 mb-5">
<!--                        <t t-esc="employee.attendance_state"/> <br/>-->
                        <a href="#"
                           t-att-data-tipo="'checked_in' if employee.attendance_state == 'checked_in' else 'checked_out'"
                           t-attf-class="btn btn-registro-hora text-muted #{employee.attendance_state == 'checked_out' and 'btn-secondary' or 'btn-warning'}">
                            <i class="fa fa-7x fa-sign-out"/>
                        </a>
                        <h5 class="mt-3 mb0 text-muted">Pulse para <b>registrar <span class="txt-check-tiempo" t-esc="'entrada' if employee.attendance_state == 'checked_out' else 'salida'"/></b></h5>
                    </div>
                    <div class="col-md-12">
                        <div id="map_registro_horas"/>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-12">
                        <a class="btn rounded-circle btn-outline-secondary" href="#">
                            Añadir registro Parte de horas
                        </a>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-md-6 col-sm-12">
                        <div id="form-registro-horas">

                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
